<div class="list-main-visual">
</div>
<div class="search-container">
	<h3><strong>音楽を見つける</strong></h3>
	<div class="keyword-search">
		<h5><strong>●●● キーワード検索 ●●●</strong></h5>
		<p>好きなキーワードで音楽を見つけることができます。カテゴリーで絞るとさらに詳細検索ができます。</p>
		<!-- 曲、アルバム、アーティストの検索窓 -->
		<%= form_tag(search_path, method: :get) do %>
			<div class="search-bar">
				<%= text_field_tag :search, "", placeholder: "キーワードを入力" %>
			</div>
			<p>カテゴリーを選択</p>
			<%= render 'layouts/radio_tile' %>
			<div class="actions">
				<%= submit_tag '見つける', class: "btn btn-danger btn-orange" %>
			</div>
		<% end %>
	</div>
	<!-- ジャンル検索 -->
	<div class="genre-search">
		<h5><strong>●●● ジャンル検索 ●●●</strong></h5>
		<p>ジャンル別で音楽を見つけることができます。</p>
		<div class="genre-items">
			<% @genres.each do |genre| %>
				<div class="genre-item">
					<%= link_to genre.name, genre_path(genre), class: "btn btn-outline-danger btn-outline-orange" %>
				</div>
			<% end %>
		</div>
	</div>
</div>
