<% if user.followed_by?(current_user) %>
    <p><%=link_to "フォロー中", user_relationships_path(user.id), method: :delete, remote: true, class: "btn btn-primary" %></p>
<% else %>
    <p><%=link_to "フォロー", user_relationships_path(user.id), method: :post, remote: true, class: "btn btn-default" %></p>
<% end %>