<% if user.followed_by?(current_user) %>
  <%=link_to "フォロー中", user_relationships_path(user.id), method: :delete, remote: true, class: "btn btn-sm bg-light btn-outline-secondary" %>
<% else %>
  <%=link_to "フォロー", user_relationships_path(user.id), method: :post, remote: true, class: "btn btn-sm bg-light btn-outline-secondary" %>
<% end %>